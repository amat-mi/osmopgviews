view_name=footway
author=Andrea Canevazzi & Luca Percich
geom_class=way
tags=highway*,footway,access,covered,surface,layer,lit,fixme::50
meta=version,tstamp::19,user_id,changeset_id::16,tags
extra_fields=round(st_length(st_transform(q.linestring, 3003))::numeric, 2) as geom_length
default_char_len=24
where=st_isvalid(linestring) and highway='footway'
description=Marciapiedi
